import { styled } from "styled-components";
import { modelStyleButtonDisable } from "../../style/globalStyle.js"

export const DivFormRegisterSyled = styled.div`

   button{
       ${modelStyleButtonDisable} 
    } 
`